# 6-nimmt

Time complexity : O(n) 
"The algorithm runs in linear time $O(N)$ by checking each card once, while using constant space $O(1)$ by saving only the single best option."

Space complexity : O(1)

Used "Greedy Algorithm" because
